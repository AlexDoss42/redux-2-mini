// import axios from 'axios'

let initialState = {
  loading: false,
  articles: []
}

export default function (state=initialState, action) {
  switch (action.type){

    default:
      return state;
  }
}